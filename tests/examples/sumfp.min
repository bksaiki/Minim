;
; Summing integers 0 to n (floating-point)
;

(def compute (n)
  (let loop ([i n] [sum 0.0])
    (if (< i 0)
        sum
        (loop (- i 1.0) (+ i sum)))))


(compute 10)
(compute 100)
(compute 1000)
(compute 10000)
(compute 100000)
