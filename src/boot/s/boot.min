;;;
;;;   Minim boot library for the bootstrap interpreter
;;;   Brett Saiki
;;;

;;;
;;;   First file loaded by the interpreter
;;;   Need to start somewhere ...
;;;

(load "base.min")
(load "expand.min")
(load "module.min")

; (define env (expander-env))
; (define syntax-rules (cdaar env))
; (define match?
;   (syntax-rules '() 
;    '((cond test clause)
;      (if test clause (void)))))
; (define result (match? '(cond #t 1)))
