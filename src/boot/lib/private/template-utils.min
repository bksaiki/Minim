;;;   
;;; template engine for `syntax`
;;;

(import "and-or.min" "cond.min" "define.min"
        "let.min" "stx.min" "when-unless.min")

(export do-template)

(define (do-template stx tstx)
  (datum->syntax
    (list
      (quote-syntax quote-syntax)
      (quote-syntax 1))))
