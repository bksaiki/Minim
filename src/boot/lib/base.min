;;;   
;;; Top-level import for base library
;;;

(import "private/and-or.min"
        "private/cond.min"
        "private/define.min"
        "private/let.min"
        "private/stx.min"
        "private/stx-case.min"
        "private/stx-macro.min"
        "private/when-unless.min")

(define-syntax (mylet stx)
  (syntax-case stx ()
    [(_ ([var val] ...) body ...) (list #'(var ...))]))

; (mylet () 1)
; (mylet ([1 2]) 1)
; (mylet ([1 2] [3 4]) 1)
; (mylet ([1 2] [3 4]) 1 2)
