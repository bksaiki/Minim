;
;   'struct'
;

(%import "../prebase.min" "../srfi/9.min")

; (%export struct)

; (def-syntax struct
;   (lambda (stx)
;     (syntax-case stx ()
;      [(_ name (ids ...))
;       ()
;       #'(begin
;           (def-record name
;             (name ids ...)
;             (id name-id 
      
