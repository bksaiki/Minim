;;;;;;;;;;;;;;;;;;; when / unless ;;;;;;;;;;;;;;;;;;;

(def-syntax when
  (lambda 
  (syntax-rules ()
   [(_ test body ...)
    (if test (begin body ...) (void))]))

(def-syntax unless
  (syntax-rules ()
   [(_ test body ...)
    (if test (void) (begin body ...))]))

(%export when unless)
