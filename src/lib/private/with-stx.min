;
;   'with-stx'
;

(%import "def.min" "let.min" "stx.min")

(%export with-syntax)

(def-syntax with-syntax
  (lambda (stx)
    (syntax-case stx ()
     [(_ ((p e0) ...) e1 e2 ...)
      (syntax (syntax-case (list e0 ...) ()
                [(p ...) (begin e1 e2 ...)]))])))
